define([
    'underscore',
    'backbone',
    'packages/options'
], function(
    _,
    Backbone,
    appOptions
    ) {
    return Backbone.Model.extend(
        {
           
        });
});